Задание

Напиши функцию generateData(n int) chan int, которая будет генерировать n чисел и передавать их через
канал.

func generateData(n int) chan int {
    // Твой код здесь
}

Пример использования функции:
func main() {
    data := generateData(10)
    go func() {
        time.Sleep(1 * time.Second) // ждем одну секунду, чтобы горутина main успела выполниться
        close(data)
    }()
    for num := range data {
        fmt.Println(num)
    }
}

Критерии приемки

Канал должен быть создан и возвращен функцией generateData.
Функция должна генерировать n чисел и передавать их через канал.
Канал должен быть закрыт после передачи всех чисел.
Все сгенерированные числа должны быть получены из канала в программе, использующей функцию
generateData.
Программа должна выводить полученные числа в консоль или использовать их для выполнения
других операций в зависимости от требований конкретной задачи.

Решение расположи по следующему пути: course1/14.chan/1.chan_using/task1.14.1.2