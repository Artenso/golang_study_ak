Задание

Реализуй две функции для работы с каналами в языке программирования Golang.

Функция mergeChan(mergeTo chan int, from …chan int)

Эта функция должна принимать канал mergeTo и переменное количество каналов from. Твоя задача —
считывать значения из всех каналов from и отправлять их в канал mergeTo. Функция должна работать до
тех пор, пока все каналы from не будут закрыты.

func mergeChan(mergeTo chan int, from ...chan int) {
    // Ваш код здесь
}

Функция mergeChan2(chans …chan int) chan int

Эта функция должна принимать переменное количество каналов chans и возвращать канал, в который
будут отправляться значения из всех каналов chans. Функция должна работать до тех пор, пока все каналы
chans не будут закрыты.

func mergeChan2(chans ...chan int) chan int {
    // Ваш код здесь
}

Вспомогательные функции

Для тестирования решения мы написали вспомогательную функцию. Ты можешь использовать её для
проверки решения.

func generateChan(n int) chan int {
    ch := make(chan int)
    go func() {
        for i := 0; i < n; i++ {
            ch <- i
        }
        close(ch)
    }()
    return ch
}

Критерии приемки

Канал mergeTo должен содержать все значения из каналов from в правильном порядке.
Функция mergeChan должна корректно работать с любым количеством каналов from.
Канал, возвращаемый функцией mergeChan2, должен содержать все значения из каналов chans в
правильном порядке.
Функция mergeChan2 должна корректно работать с любым количеством каналов chans.
Все каналы from и chans должны быть закрыты после завершения работы функций mergeChan и
mergeChan2 соответственно.

Решение расположи по следующему пути: course1/14.chan/1.chan_using/task1.14.1.1