Задание

Реализуй семафор с помощью каналов в языке программирования Golang. У тебя есть код, который
содержит структуру sema и методы New, Inc и Dec. Задача состоит в том, чтобы реализовать логику
семафора, чтобы он правильно управлял доступом к ресурсам.

package main

import (
	"fmt"
)

type sema chan struct{}

func New(n int) sema {
}

func (s sema) Inc(k int) {
}

func (s sema) Dec(k int) {
}

func main() {
	numbers := []int{1, 2, 3, 4, 5}
	n := len(numbers)
	sem := New(n)
	for _, num := range numbers {
		go func(n int) {
			fmt.Println(n)
			sem.Inc(1)
		}(num)
	}
	sem.Dec(n)
}

Реализована структура sema с помощью каналов.
Метод New создает семафор с заданным количеством ресурсов.
Метод Inc увеличивает количество доступных ресурсов на указанное значение.
Метод Dec уменьшает количество доступных ресурсов на указанное значение.
Горутины правильно используют семафор для доступа к ресурсам.
Программа выводит числа из списка numbers в правильном порядке.
Программа завершается после того, как все горутины завершат свою работу.

Решение расположи по следующему пути: course1/15.goroutines/3.goroutines_chan/task1.15.3.3