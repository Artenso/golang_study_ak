Задание

У тебя есть код программы на языке Golang. Твоя задача — изменить блок отложенного выполнения (defer
block) таким образом, чтобы он возвращал результат умножения чисел a и b вместо их суммы.

package main

import "fmt"

func main() {
	a, b := 8, 13
	fmt.Println(*testDefer(&a, &b))
}

func testDefer(a, b *int) *int {
	var c int
	defer func() {
	}()
	c = sum(*a, *b)
	return &c
}

func sum(a, b int) int {
	return a + b
}

func multiply(a, b int) int {
	return a * b
}


Критерии приемки:

Результат выполнения программы должен быть равен 104

Решение расположи по следующему пути: course1/15.gorounes/4.gorounes_defer/task1.15.4.2