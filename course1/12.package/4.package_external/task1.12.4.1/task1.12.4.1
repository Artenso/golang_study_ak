Задание

Создай внешний пакет с именем mymath, который будет содержать функции для выполнения
математических операций.

Внешний пакет:

// Пример кода для иллюстрации задачи
package mymath

import "math"

// Sqrt - функция для вычисления квадратного корня из числа
func Sqrt(x float64) int {
    return math.Sqrt(x)
}
...

Основное приложение:

package main

import (
    "fmt"
    "gitlab.com/username/mymath"
)

func main() {
    fmt.Println(mymath.Sqrt(4)) // 2
}

Критерии приемки

Пакет должен называться mymath.
Пакет реализует все функции базового пакета math.
Пакет mymath находится во внешнем модуле gitlab.com/username/mymath.
Пакет mymath импортируется в функции main.
Функционал локального пакета mymath должен повторять функционал встроенного пакета math.
Функции с примерами использования должны быть использованы в функции main.

Решение расположи по следующему пути: course1/12.package/4.package_external/task1.12.4.1