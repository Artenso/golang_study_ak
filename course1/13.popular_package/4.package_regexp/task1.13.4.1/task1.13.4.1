Задание

Напиши программу на языке Go, которая принимает на вход строку и проверяет, является ли она
валидным email-адресом с использованием регулярных выражений.

package main

import (
    "fmt"
    "regexp"
)

func main() {
    email := "test@example.com"
    valid := isValidEmail(email)
    if valid {
        fmt.Printf("%s является валидным email-адресом\n", email)
    } else {
        fmt.Printf("%s не является валидным email-адресом\n", email)
    }
}

func isValidEmail(email string) bool {
    /...
}

Критерии приемки

Строка “test@example.com” должна быть распознана как валидный email-адрес.
Строка “invalid_email” должна быть распознана как невалидный email-адрес.
В программе должно использоваться регулярное выражение для проверки email-адреса.
Программа должна выводить сообщение о валидности или невалидности email-адреса.