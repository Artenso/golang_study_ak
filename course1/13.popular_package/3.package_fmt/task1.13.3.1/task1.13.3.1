Задание

Напиши функцию для генерации строки с математическими операциями с использованием fmt.Sprintf().
Функция должна принимать входные параметры и возвращать сформированную строку с операциями.

// Пример кода, если необходимо
func generateMathString(operands []int, operator string) string {
    // Твой код здесь
}

// Пример результата выполнения программы:
func main() {
    fmt.Println(generateMathString([]int{2, 4, 6}, "+")) // "2 + 4 + 6 = 12"
}

Критерии приемки

Функция должна принимать входные параметры — массив операндов и оператор.
Функция должна использовать fmt.Sprintf() для форматирования строки с операциями.
Сформированная строка должна содержать все операнды и операторы в правильном порядке.
Функция должна возвращать сформированную строку с математическими операциями.

Решение расположи по следующему пути: course1/13.popular_package/3.package_fmt/task1.13.3.1