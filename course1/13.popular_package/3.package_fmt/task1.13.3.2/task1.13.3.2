Задание

Напиши функцию, которая будет возвращать тип переменной с использованием функции fmt.Sprintf().
Функция должна принимать переменную в качестве аргумента и возвращать ее тип в виде строки.

// Пример использования функции
func main() {
    var num int = 10
    var str string = "Hello"
    fmt.Println(getVariableType(num)) // Вывод: "int"
    fmt.Println(getVariableType(str)) // Вывод: "string"
}

// Функция для получения типа переменной
func getVariableType(variable interface{}) string {
    //...
}

Критерии приемки

Функция должна называться getVariableType.
Функция должна принимать один аргумент типа interface{}.
Функция должна возвращать тип переменной в виде строки, используя функцию fmt.Sprin().
Программа должна выводить результат вызова функции getVariableType для различных типов
переменных.
Результаты должны быть корректными и соответствовать ожидаемым типам переменных.

Решение расположи по следующему пути: course1/13.popular_package/3.package_fmt/task1.13.3.2