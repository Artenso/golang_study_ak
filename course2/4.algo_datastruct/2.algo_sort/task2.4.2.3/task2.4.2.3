Задание

Создай функцию Merge, которая будет сливать два отсортированных массива пользователей в один
отсортированный массив.

// Структура пользователя
type User struct {
ID int
Name string
Age int
}
// Функция слияния двух отсортированных массивов пользователей
func Merge(arr1 []User, arr2 []User) []User {
// Ваш код здесь
}

Критерии приемки

Функция Merge принимает два отсортированных массива пользователей и возвращает отсортированный массив.
Возвращаемый массив содержит всех пользователей из обоих исходных массивов.
Пользователи в возвращаемом массиве отсортированы по возрастанию их ID.
Если один из исходных массивов пуст, функция Merge возвращает другой массив без изменений.
Функция Merge эффективна по времени и не использует дополнительную память, кроме памяти,
необходимой для хранения результирующего массива.
Код покрыт тестами на 100%.

Решение расположи по следующему пути: course2/4.algo_datastruct/2.algo_sort/task2.4.2.3