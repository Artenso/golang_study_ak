Задание

Тебе предоставлен код кольцевого буфера (RingBuffer) на языке программирования Golang.

package main
import "fmt"
type CircuitRinger interface {
Add(val int)
Get() (int, bool)
}
func main() {
rb := NewRingBuffer(3)
rb.Add(1)
rb.Add(2)
rb.Add(3)
rb.Add(4) // Перезаписывает значение 1
for val, ok := rb.Get(); ok; val, ok = rb.Get() {
fmt.Println(val) // Выводит: 2, 3, 4
}
if _, ok := rb.Get(); !ok {
fmt.Println("Buffer is empty") // Выводит: Buffer is empty
}
}

Критерии приемки

Кольцевой буфер имеет метод Add, который добавляет значение в буфер.
Кольцевой буфер имеет метод Get, который возвращает значение из буфера.
Если буфер пуст, метод Get возвращает false вместе с нулевым значением.
Если буфер заполнен, новые значения заменяют старые значения в порядке FIFO (First-In, First-Out).
При добавлении нового значения, если буфер заполнен, старое значение удаляется из буфера.
После добавления значений 1, 2, 3 и 4 в буфер, метод Get возвращает значения 2, 3 и 4 в указанном
порядке.
При попытке извлечения значения из пустого буфера выводится сообщение «Буфер пуст».

Подсказка: этот алгоритм используется в реализации каналов (channels) в языке программирования Golang.

Решение расположи по следующему пути: course2/4.algo_datastruct/5.datastruct_queue/task2.4.5.5